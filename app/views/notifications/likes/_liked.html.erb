<% if defined? notification.notifiable.post_id %>
  <%= link_to post_path(notification.notifiable.post_id), class: "dropdown-item #{"unread" if !notification.read_at? }" do %>
    <%= notification.user.name %>
    <%= notification.action %>
    your post
  <% end %>
<% end %>
